package client

/*
func TestAuthCodeURL(t *testing.T) {
	assert := assert.New(t)

	oauth2Cfg, err := setupTestEnvOAuth2Config(false)
	if !assert.NoError(err) {
		return
	}

	t.Log(oauth2Cfg.AuthCodeURL("64992244-fcbd-460a-9520-03e6ab292843"))
}

func TestExchangeCodeURL(t *testing.T) {
	assert := assert.New(t)

	oauth2Cfg, err := setupTestEnvOAuth2Config(false)
	if !assert.NoError(err) {
		return
	}

	token, err := oauth2Cfg.Exchange(context.Background(), os.Getenv("EFACTURA_TEST_EXCHANGE_CODE"))
	if !assert.NoError(err) {
		return
	}

	jtoken, err := json.Marshal(token)
	if !assert.NoError(err) {
		return
	}

	t.Log(string(jtoken))
}
*/
